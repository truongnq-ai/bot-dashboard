/**
 * UI Configuration
 * 
 * Centralized configuration for UI components and interactions
 */

/**
 * Button Loading State Configuration
 */
export const BUTTON_LOADING_CONFIG = {
  /**
   * Interval (in milliseconds) for loading dots animation
   * Controls how fast the dots change: . → .. → ... → .
   * Default: 500ms
   */
  DOTS_ANIMATION_INTERVAL: 1000,
} as const;
